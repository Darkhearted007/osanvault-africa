{
  "name": "@osanvault/programs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "anchor test",
    "test:core": "cd osanvault_core && anchor test",
    "test:lend": "cd osanvault_lend && anchor test",
    "test:reits": "cd reits && anchor test",
    "test:landbank": "cd landbank && anchor test",
    "test:minerals": "cd minerals && anchor test",
    "test:carbon": "cd carbon && anchor test",
    "test:oracle": "cd oracle && anchor test",
    "build": "anchor build",
    "build:all": "pnpm -r --parallel build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.4.0",
    "mocha": "^10.4.0"
  }
}