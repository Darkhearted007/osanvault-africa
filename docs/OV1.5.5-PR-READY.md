# OV1.5.5 PR Review Gate

Branch: `feat/identity-foundation`

Review focus:

- identity schema correctness;
- migration/schema parity;
- additive safety;
- provider-neutral principal model;
- authorization primitive behavior;
- compatibility with existing schema.

Do not merge to `main` until staging PostgreSQL execution and regression validation are complete.
